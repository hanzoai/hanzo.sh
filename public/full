#!/bin/sh
# Every Hanzo tool that ships as a public native binary — which is what
#   curl -fsSL hanzo.sh | sh
# already installs. Kept so an existing hanzo.sh/full keeps working; there is
# one install path, not two.
exec sh -c "$(curl -fsSL https://hanzo.sh)" -- "$@"
